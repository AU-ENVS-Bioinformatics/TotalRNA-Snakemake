import pandas as pd
import re
from pathlib import Path

from targets.qc import qc_outputs
from targets.rna_seperation import (
    rna_outputs,
    sortmerna_stages,
    previous_stage_map,
    sortmerna_input_r1,
    sortmerna_input_r2,
    get_sortmerna_db,
    get_sortmerna_db_idx,
)
from targets.rrna import rrna_outputs
from targets.nonrrna import non_rrna_outputs

configfile: "config/config.yaml"

# --------------------------
# Load samplesheet
# --------------------------
samplesheet = pd.read_csv(
    config["samples_file"],
    sep=r"\s+",
    dtype=str,
    engine="python"
)

samplesheet.columns = samplesheet.columns.str.strip()
samplesheet = samplesheet.set_index("sample_id")

SAMPLES = samplesheet.index.tolist()

RESULTS_DIR = Path(config["shared_results_dir"])

print(f"{len(SAMPLES)} samples loaded")
print(f"Shared results directory: {RESULTS_DIR}")

# --------------------------
# Helpers (raw reads)
# --------------------------

def raw_r1(wc):
    r1 = samplesheet.loc[wc.sample, "R1"]
    print(f"[INFO] sample: {wc.sample}: read 1 = {r1}")
    return r1

def raw_r2(wc):
    r2 = samplesheet.loc[wc.sample, "R2"]
    print(f"[INFO] sample: {wc.sample}: read 2 = {r2}")
    return r2

required_cols = ["R1", "R2"]

missing = [c for c in required_cols if c not in samplesheet.columns]

if missing:
    raise ValueError(
        f"Samplesheet is missing required columns: {', '.join(missing)}"
    )

for sample in samplesheet.index:
    for col in ["R1", "R2"]:
        if pd.isna(samplesheet.loc[sample, col]):
            raise ValueError(
                f"Sample {sample} is missing {col}"
            )

def sample_qc_dir(wc):
    return RESULTS_DIR / "qc" / wc.sample

def rrna_dir(*paths):
    return str(Path(RESULTS_DIR, "rRNA", *paths))


# --------------------------
# SortMeRNA stage logic
# --------------------------

SORTMERNA_STAGES = sortmerna_stages(config)
PREVIOUS_STAGE = previous_stage_map(SORTMERNA_STAGES)

FIRST_STAGE_NAME = SORTMERNA_STAGES[0]
FINAL_STAGE_NAME = SORTMERNA_STAGES[-1]

FIRST_SORTMERNA_STAGE = [FIRST_STAGE_NAME for _ in SAMPLES]
FINAL_SORTMERNA_STAGE = [FINAL_STAGE_NAME for _ in SAMPLES]

print(f"sortmerna stages {SORTMERNA_STAGES}")
print(f"previous stage mapping {PREVIOUS_STAGE}")

# --------------------------
# SortMeRNA wrappers (wc-aware)
# --------------------------

def sortmerna_r1(wc):
    return sortmerna_input_r1(
        sample=wc.sample,
        stage=wc.stage,
        results_dir=RESULTS_DIR,
        previous_stage=PREVIOUS_STAGE,
    )


def sortmerna_r2(wc):
    return sortmerna_input_r2(
        sample=wc.sample,
        stage=wc.stage,
        results_dir=RESULTS_DIR,
        previous_stage=PREVIOUS_STAGE,
    )


def sortmerna_db(wc):
    return get_sortmerna_db(wc.stage, config)


def sortmerna_db_idx(wc):
    return get_sortmerna_db_idx(wc.stage, config)


# --------------------------
# Final target
# --------------------------

rule all:
    input:
        f"{RESULTS_DIR}/qc/QC.done",
        f"{RESULTS_DIR}/RNA/RNA_seperation.done",
        f"{RESULTS_DIR}/rRNA/rRNA.done",
        f"{RESULTS_DIR}/nonrRNA/nonrRNA.done"

# --------------------------
# Modules
# --------------------------

### Quality control and decontamination
include: "qc/rules/fastqc.smk"
include: "qc/rules/fastp.smk"
include: "qc/rules/bwa_decontamination.smk"

### RNA SEPERATION - check if the if statements are redudnant with the ones in the included rules (smk files) or if they are needed to avoid including rules that would not be used
if config["RNA"]["method"] == "sortmerna":
    if config["RNA"]["refinement"] == "staged":
        include: "RNA/rules/sortmerna_stages.smk"
        include: "RNA/rules/sortmerna_stages_postprocessing.smk"
    if config["RNA"]["refinement"] == "combined":
        include: "RNA/rules/sortmerna_combined.smk"
        include: "RNA/rules/sortmerna_combined_postprocessing.smk"
elif config["RNA"]["method"] == "ribodetector":
    include: "RNA/rules/ribodetector.smk"
    if config["RNA"]["refinement"] == "bbduk":
        include: "RNA/rules/bbduk.smk"
elif config["RNA"]["method"] == "bbmap":
    include: "RNA/rules/bbmap.smk"
    include: "RNA/rules/bbmap_postprocessing.smk"

### rRNA composition classification
#include: "rRNA/rules/qiime2.smk"
include: "rRNA/rules/kraken.smk"
include: "rRNA/rules/bracken.smk"
include: "rRNA/rules/kraken_bracken_downstream.smk"

### non-rRNA read-based functional profiling / ORF functional annotation
include: "nonrRNA/rules/preprocessing.smk"

# --------------------------
# non-rRNA module selection
# --------------------------
if config["nonrRNA"]["module"] == "read":
    if config["nonrRNA"]["method"] in ["metaphlan", "humann"]:
        # Metaphlan is ALWAYS required (taxonomy baseline)
        include: "nonrRNA/rules/metaphlan.smk"

        # HUMANN is ALWAYS downstream of metaphlan in your design
        include: "nonrRNA/rules/humann.smk"
        include: "nonrRNA/rules/humann_postprocessing.smk"
elif config["nonrRNA"]["module"] == "coassembly":
    if config["nonrRNA"]["method"] in ["spades","rnaspades"]:
        # Co assembly
        include: "nonrRNA/rules/co_assembly.smk"
    if config["nonrRNA"]["predictor"] in ["transdecoder"]:
        include: "nonrRNA/rules/diamond.smk"
        include: "nonrRNA/rules/hmmscan.smk"
        include: "nonrRNA/rules/ORF_prediction.smk" #for now we need ORF_prediction no matter what - NB! consider moving this out to sample specific assembly if this is introduced
else:
    raise ValueError(f"Unsupported nonrRNA method: {config['nonrRNA']['method']}")

"""
if config["nonrRNA"]["method"] == "read":
    include: "nonrRNA/rules/metaphlan.smk"
    include: "nonrRNA/rules/humann.smk"
    include: "nonrRNA/rules/humann_postprocessing.smk"
elif config["nonrRNA"]["method"] == "coassembly":
    include: "nonrRNA/rules/assembly.smk"
    include: "nonrRNA/rules/ORF_prediction.smk"
    include: "nonrRNA/rules/eggnog.smk"
    include: "nonrRNA/rules/alignment.smk"
    include: "nonrRNA/rules/abundance.smk"

elif config["nonrRNA"]["method"] == "assembly":
    config["nonrRNA"]["method"]["refinement"] == "viral": # also co assembly
    config["nonrRNA"]["method"]["refinement"] == "coassembly":
    config["nonrRNA"]["method"]["refinement"] == "sample":
"""

# once the rules has been completed check 
# --------------------------
# Final targets
# --------------------------
rule QC_done:
    input:
        qc_outputs(RESULTS_DIR, SAMPLES)
    output:
        flag=f"{RESULTS_DIR}/qc/QC.done"
    shell:
        """
        mkdir -p {RESULTS_DIR}/qc
        touch {output.flag}
        """

rule RNA_seperation_done:
    input:
        rna_outputs(RESULTS_DIR, SAMPLES, config, FINAL_SORTMERNA_STAGE)
    output:
        flag=f"{RESULTS_DIR}/RNA/RNA_seperation.done"
    shell:
        """
        mkdir -p {RESULTS_DIR}/RNA
        touch {output.flag}
        """

rule rRNA_done:
    input:
        rrna_outputs(RESULTS_DIR, SAMPLES,config)
    output:
        flag=f"{RESULTS_DIR}/rRNA/rRNA.done"
    shell:
        """
        mkdir -p {RESULTS_DIR}/rRNA
        touch {output.flag}
        """

rule nonrRNA_done:
    input:
        non_rrna_outputs(RESULTS_DIR,SAMPLES,config)
    output:
        flag=f"{RESULTS_DIR}/nonrRNA/nonrRNA.done"
    shell:
        """
        mkdir -p {RESULTS_DIR}/nonrRNA
        touch {output.flag}
        """


# snakemake --use-conda --conda-prefix /data_2/rasmus/snakemake-conda --cores 35 --rerun-incomplete --keep-going
# snakemake --use-conda --conda-prefix /data_2/rasmus/snakemake-conda --configfile config/config.yaml --cores 35 --rerun-incomplete --keep-going --dry-run
# snakemake --use-conda --conda-prefix /data_2/rasmus/snakemake-conda --configfile config/config.yaml --cores 35 --rerun-incomplete --keep-going --config samples_file=/data/rasmus/TotalRNA-Snakemake/dataset/samplesheet_v2.tsv

#mamba create -n eggnog_env_dev -c conda-forge -c bioconda eggnog-mapper=2.1.13
#mamba create -n prodigal_env -c bioconda prodigal
#mamba create -n transdecoder_env -c bioconda transdecoder